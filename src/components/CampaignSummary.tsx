import type{WorldState}from'../game/world-state';import{campaignReport}from'../game/campaign-report';import{smartPlan}from'../game/smart-plan';import{legacyPreview}from'../game/legacy-preview';export function CampaignSummary({state,onSchedule}:{state:WorldState;onSchedule:()=>void}){const report=campaignReport(state),plan=smartPlan(state),legacy=legacyPreview(state);return <aside className="campaign-summary"><div><small>{report.chapter} · {report.month}/12개월</small><b>{report.headline}</b></div><div className="campaign-meters"><span>월드 <b>{report.worldCompletion}%</b></span><span>수집 <b>{report.collectionCompletion}%</b></span><span>유대 <b>{report.relationship}%</b></span></div><div className="campaign-next"><small>이번 달 추천</small><b>{plan.worldReason}</b></div><div className="campaign-legacy"><small>예상 성장 방향</small><b>{legacy.title}</b><span>{legacy.summary}</span></div><button onClick={onSchedule}>일정 짜기</button></aside>}
