import{renderToStaticMarkup}from'react-dom/server';import{describe,expect,it,vi}from'vitest';import{initialWorldState}from'../game/world-state';import{WorldHub}from'./WorldHub';describe('world hub',()=>{it('keeps expansion behind one compact home entry',()=>{const html=renderToStaticMarkup(<WorldHub state={initialWorldState} dispatch={vi.fn()}/>);expect(html).toContain('모험');expect(html).toContain('0/5');expect(html).toContain('목표 0/4');expect(html).not.toContain('루나와 더 넓은 세상으로')})});
