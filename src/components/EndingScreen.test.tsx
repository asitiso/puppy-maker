import {renderToStaticMarkup} from 'react-dom/server';import {describe,expect,it,vi} from 'vitest';import {initialState} from '../game';import {EndingScreen} from './EndingScreen';describe('ending screen',()=>{it('shows resolved ending and persistent discovery count',()=>{const html=renderToStaticMarkup(<EndingScreen state={{...initialState,screen:'ending',monthsCompleted:12,resolvedEnding:'guardian',endingCollection:['guardian']}} dispatch={vi.fn()}/>);expect(html).toContain('별빛의 수호자');expect(html).toContain('1/6');expect(html).toContain('새로운 이야기')})});
