import {renderToStaticMarkup} from 'react-dom/server';import {describe,expect,it,vi} from 'vitest';import {initialState} from '../game';import {StoryEvent} from './StoryEvent';describe('story event presentation',()=>{it('renders active event title body and choices',()=>{const html=renderToStaticMarkup(<StoryEvent state={{...initialState,screen:'event',activeEventId:'lost_bird'}} dispatch={vi.fn()}/>);expect(html).toContain('숲속의 작은 손님');expect(html).toContain('날개를 다친');expect(html).toContain('함께 돌봐준다');expect(html).toContain('/assets/runa/runa_surprised.png')})});
