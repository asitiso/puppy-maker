import{renderToStaticMarkup}from'react-dom/server';import{describe,expect,it,vi}from'vitest';import{initialWorldState}from'../game/world-state';import{CampaignSummary}from'./CampaignSummary';describe('campaign summary',()=>{it('renders one compact actionable campaign card',()=>{const html=renderToStaticMarkup(<CampaignSummary state={initialWorldState} onSchedule={vi.fn()}/>);expect(html).toContain('이번 달 추천');expect(html).toContain('예상 성장 방향');expect(html).toContain('일정 짜기')})});
