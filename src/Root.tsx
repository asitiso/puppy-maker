import type{Dispatch}from'react';import{GameApp}from'./App';import LayeredHome from'./LayeredHome';import{ProgressRibbon}from'./components/ProgressRibbon';import{WorldHub}from'./components/WorldHub';import{CampaignSummary}from'./components/CampaignSummary';import{useWorldGameState}from'./game/use-world-game';import type{Action}from'./game';import'./layered-home.css';import'./home-panels.css';import'./world-hub.css';import'./campaign-summary.css';export default function Root(){const{state,dispatch}=useWorldGameState();const showHome=state.screen==='hub';const baseDispatch=dispatch as Dispatch<Action>;const schedule=()=>dispatch({type:'GO',screen:'schedule'});return <><GameApp state={state} dispatch={baseDispatch} renderHub={!showHome}/>{showHome&&<><LayeredHome state={state} onSchedule={schedule}/><ProgressRibbon state={state}/><CampaignSummary state={state} onSchedule={schedule}/><WorldHub state={state} dispatch={dispatch}/></>}</>}
