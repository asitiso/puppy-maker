import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure record independence',()=>{it('tracks destinations independently',()=>{const a=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:900,mastered:true});const b=worldReducer(a,{type:'EXPLORE',destinationId:'moon_garden',score:600});expect(b.adventureRecords.forest_path).toMatchObject({bestScore:900,mastered:true});expect(b.adventureRecords.moon_garden).toMatchObject({bestScore:600,mastered:false})})});
