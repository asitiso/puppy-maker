import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure result messaging',()=>{it('keeps grade and score in durable adventure memory even when an achievement toast takes priority',()=>{const state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:820});expect(state.lastWorldMessage).toContain('업적 달성');expect(state.worldMoments.at(-1)?.summary).toContain('A등급 820점')})});
