import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure result messaging',()=>{it('includes grade and score for a scored clear',()=>{const state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:820});expect(state.lastWorldMessage).toContain('등급');expect(state.worldMoments.at(-1)?.summary).toContain('820점')})});
