import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world bond cap',()=>{it('rejects a fourth monthly bond action without adding a memory',()=>{const capped={...initialWorldState,monthlyBondActions:3,worldMoments:[]};const next=worldReducer(capped,{type:'BOND'});expect(next).toBe(capped);expect(next.worldMoments).toHaveLength(0)})});
