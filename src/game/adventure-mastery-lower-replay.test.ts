import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure mastery lower-grade replay',()=>{it('keeps MASTER even when later result is only C',()=>{const master=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});const later=worldReducer({...master,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:50});expect(later.adventureRecords.forest_path.mastered).toBe(true);expect(later.adventureRecords.forest_path.bestGrade).toBe('S')})});
