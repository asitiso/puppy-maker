import{describe,expect,it}from'vitest';import{initialWorldState,hydrateWorldState}from'./world-state';describe('adventure plays migration',()=>{it('normalizes malformed negative play counts',()=>{const state=hydrateWorldState(JSON.stringify({...initialWorldState,adventureRecords:{forest_path:{bestScore:100,bestGrade:'C',firstCleared:true,mastered:false,plays:-9}}}));expect(state.adventureRecords.forest_path.plays).toBe(0)})});
