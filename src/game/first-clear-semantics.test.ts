import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('first clear record semantics',()=>{it('keeps unscored legacy exploration distinct from a scored first clear',()=>{const state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path'});expect(state.discoveredDestinations).toContain('forest_path');expect(state.adventureRecords.forest_path).toMatchObject({firstCleared:false,mastered:false,plays:0})})});
