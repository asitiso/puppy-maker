import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world progression invariants',()=>{it('keeps stats inside bounds after repeated world actions',()=>{let state=initialWorldState;for(let i=0;i<40;i++)state=worldReducer({...state,monthlyBondActions:0},{type:'BOND'});for(const value of Object.values(state.stats))expect(value).toBeGreaterThanOrEqual(0);for(const value of Object.values(state.stats))expect(value).toBeLessThanOrEqual(100)})});
