import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world message safety',()=>{it('clears transient feedback without touching progression',()=>{const state={...initialWorldState,lastWorldMessage:'MASTER!',adventureMasteries:['forest_path']};const next=worldReducer(state,{type:'CLEAR_WORLD_MESSAGE'});expect(next.lastWorldMessage).toBeUndefined();expect(next.adventureMasteries).toEqual(['forest_path'])})});
