import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure mastery world score',()=>{it('makes a first MASTER worth more world score than a non-master clear at the same grade',()=>{const clear=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:false});const master=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(master.monthlyWorldScore).toBeGreaterThan(clear.monthlyWorldScore)})});
