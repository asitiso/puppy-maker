import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('best world score',()=>{it('preserves historical best across monthly reset',()=>{const before={...initialWorldState,screen:'result' as const,monthlyWorldScore:80,bestWorldScore:120};const after=worldReducer(before,{type:'NEXT_MONTH'});expect(after.monthlyWorldScore).toBe(0);expect(after.bestWorldScore).toBe(120)})});
