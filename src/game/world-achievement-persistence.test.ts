import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world achievement persistence',()=>{it('preserves unlocked world achievements across a new run',()=>{const before={...initialWorldState,worldAchievements:['first_step','adventure_apprentice']};const after=worldReducer(before,{type:'NEW_RUN'});expect(after.worldAchievements).toEqual(['first_step','adventure_apprentice'])})});
