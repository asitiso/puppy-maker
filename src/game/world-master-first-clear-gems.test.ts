import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world master first clear gems',()=>{it('gives a master S first clear more gems than an ordinary clear',()=>{const ordinary=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:700});const master=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(master.gems).toBeGreaterThan(ordinary.gems)})});
