import{describe,expect,it}from'vitest';import{initialWorldState,hydrateWorldState}from'./world-state';describe('world numeric migration',()=>{it('clamps negative persisted world counters to zero',()=>{const state=hydrateWorldState(JSON.stringify({...initialWorldState,monthlyExplorations:-2,totalCooking:-4,totalExplorations:-5,bondXp:-7,bestWorldScore:-9}));expect(state).toMatchObject({monthlyExplorations:0,totalCooking:0,totalExplorations:0,bondXp:0,bestWorldScore:0})})});
