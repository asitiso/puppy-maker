import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world progression feedback',()=>{it('keeps immediate feedback transient and memories persistent',()=>{const explored=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:800});expect(explored.lastWorldMessage).toBeTruthy();const cleared=worldReducer(explored,{type:'CLEAR_WORLD_MESSAGE'});expect(cleared.lastWorldMessage).toBeUndefined();expect(cleared.worldMoments).toHaveLength(1)})});
