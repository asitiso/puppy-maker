import{describe,expect,it}from'vitest';import{destinations}from'./exploration';import{adventureStage}from'./adventure/catalog';import{ruleForStage}from'./adventure/rules';describe('all adventure stage contracts',()=>{it('gives every destination a playable stage and matching unlock month',()=>{expect(destinations).toHaveLength(10);for(const destination of destinations){const stage=adventureStage(destination.id);expect(stage,`${destination.id} stage`).toBeTruthy();expect(stage?.destinationId).toBe(destination.id);expect(stage?.unlockMonth).toBe(destination.unlockMonth);expect(stage?.duration).toBeGreaterThan(0);expect(ruleForStage(destination.id)).toBeTruthy()}})});
