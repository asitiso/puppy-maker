import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure reward record consistency',()=>{it('keeps mastery and S collections aligned with a master S clear',()=>{const state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(state.adventureSGrades).toContain('forest_path');expect(state.adventureMasteries).toContain('forest_path');expect(state.adventureRecords.forest_path).toMatchObject({bestGrade:'S',mastered:true,firstCleared:true})})});
