import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure attempt cap',()=>{it('allows two adventures per month and rejects the third without mutating records',()=>{let state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:800});state=worldReducer(state,{type:'EXPLORE',destinationId:'moon_garden',score:800});const before=JSON.stringify(state.adventureRecords);const third=worldReducer(state,{type:'EXPLORE',destinationId:'village_market',score:950,mastered:true});expect(third).toBe(state);expect(JSON.stringify(third.adventureRecords)).toBe(before)})});
