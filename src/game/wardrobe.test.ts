import{describe,expect,it}from'vitest';import{unlockedWardrobe,wardrobe}from'./wardrobe';describe('wardrobe collection',()=>{it('starts with canonical Runa collectible only',()=>expect(unlockedWardrobe({monthsCompleted:0,discoveredDestinations:[],huntChallengeClears:[],endingCollection:[]})).toEqual(['runa_classic']));it('unlocks collection badges from real progression',()=>{const ids=unlockedWardrobe({monthsCompleted:12,discoveredDestinations:['forest_path','moon_garden','old_shrine'],huntChallengeClears:['a','b'],endingCollection:['guardian']});expect(ids).toHaveLength(wardrobe.length);expect(ids).toContain('starlight_crown')})});
