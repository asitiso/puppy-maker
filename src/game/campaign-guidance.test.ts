import{describe,expect,it}from'vitest';import{campaignReport}from'./campaign-report';import{initialWorldState}from'./world-state';describe('campaign guidance',()=>{it('prioritizes exploration while attempts remain',()=>{expect(campaignReport(initialWorldState).nextFocus).toContain('탐험')});it('celebrates complete mastery',()=>{const state={...initialWorldState,adventureMasteries:Array(11).fill('master')};expect(campaignReport(state).headline).toContain('모든 모험')})});
