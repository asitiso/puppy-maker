import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure score safety',()=>{it('keeps best score and grade monotonic across weaker replays',()=>{const strong=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:940,mastered:false});const weak=worldReducer({...strong,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:200,mastered:false});expect(weak.adventureRecords.forest_path.bestScore).toBe(940);expect(weak.adventureRecords.forest_path.bestGrade).toBe('S')});it('never grants negative gold for a scored adventure',()=>{const next=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:0,mastered:false});expect(next.gold).toBeGreaterThanOrEqual(initialWorldState.gold)})});
