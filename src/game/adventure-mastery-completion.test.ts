import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure mastery completion',()=>{it('supports a ten-stage mastery collection without duplicates',()=>{const ids=['forest_path','moon_garden','village_market','old_shrine','starlight_hill','crystal_creek','whispering_grove','sunset_field','cloud_bridge','guardian_sanctum'];const state={...initialWorldState,adventureMasteries:ids,adventureSGrades:ids};const fresh=worldReducer(state,{type:'NEW_RUN'});expect(fresh.adventureMasteries).toHaveLength(10);expect(new Set(fresh.adventureMasteries).size).toBe(10)})});
