import { describe, expect, it } from 'vitest'; import { advanceCalendar,seasonForMonth,milestoneForMonth,milestoneForCompletedMonths } from './progression';
describe('long-run progression',()=>{it('rolls December into next year',()=>expect(advanceCalendar(1,12)).toEqual({year:2,month:1}));it('maps months to four seasons',()=>{expect(seasonForMonth(3)).toBe('spring');expect(seasonForMonth(7)).toBe('summer');expect(seasonForMonth(10)).toBe('autumn');expect(seasonForMonth(1)).toBe('winter')});it('marks calendar and completion milestones',()=>{expect(milestoneForMonth(3)).toBe('first-quarter');expect(milestoneForCompletedMonths(3)).toBe('first-quarter');expect(milestoneForCompletedMonths(6)).toBe('half-year');expect(milestoneForCompletedMonths(9)).toBe('final-quarter');expect(milestoneForCompletedMonths(12)).toBe('final-month');expect(milestoneForCompletedMonths(5)).toBeNull()})});
