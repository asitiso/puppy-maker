import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world adventure replay discovery reward',()=>{it('keeps discovery list stable on replay',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:500});const second=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:500});expect(second.discoveredDestinations).toEqual(first.discoveredDestinations)})});
