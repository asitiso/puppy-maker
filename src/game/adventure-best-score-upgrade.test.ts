import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure best score upgrade',()=>{it('updates best score on improvement while increasing play count',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:500});const second=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:800});expect(second.adventureRecords.forest_path.bestScore).toBe(800);expect(second.adventureRecords.forest_path.plays).toBe(2)})});
