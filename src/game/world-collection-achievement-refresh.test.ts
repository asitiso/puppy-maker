import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world collection achievement refresh',()=>{it('does not award the same world achievement twice',()=>{const state={...initialWorldState,worldAchievements:['first_step'],discoveredDestinations:['forest_path'],monthlyExplorations:0};const next=worldReducer(state,{type:'EXPLORE',destinationId:'forest_path',score:500});expect(next.worldAchievements.filter(id=>id==='first_step')).toHaveLength(1)})});
