import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('locked adventure safety',()=>{it('rejects destinations before their unlock month',()=>{const next=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'guardian_sanctum',score:999,mastered:true});expect(next).toBe(initialWorldState);expect(next.adventureMasteries).toEqual([])})});
