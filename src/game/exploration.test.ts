import{describe,expect,it}from'vitest';import{availableDestinations,explorationReward}from'./exploration';describe('exploration RED',()=>{it('requires exploration domain',()=>{expect(availableDestinations(1).map(x=>x.id)).toEqual(['forest_path']);expect(explorationReward('forest_path',[])?.firstDiscovery).toBe(true)})});
