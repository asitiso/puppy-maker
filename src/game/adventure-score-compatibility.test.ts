import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure score compatibility',()=>{it('continues updating legacy best-score map for existing UI consumers',()=>{const state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:777});expect(state.adventureBestScores.forest_path).toBe(777);expect(state.adventureRecords.forest_path.bestScore).toBe(777)})});
