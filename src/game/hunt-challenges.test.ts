import{describe,expect,it}from'vitest';import{availableHuntChallenges,challengeRank,challengeCleared}from'./hunt-challenges';describe('hunt challenges',()=>{it('uses month and hunt mastery to unlock challenges',()=>{expect(availableHuntChallenges(2,1)).toHaveLength(1);expect(availableHuntChallenges(11,4)).toHaveLength(4)});it('ranks scores predictably',()=>{expect(challengeRank(162,120)).toBe('S');expect(challengeRank(120,120)).toBe('A');expect(challengeRank(90,120)).toBe('B');expect(challengeRank(20,120)).toBe('C')});it('requires target score to clear',()=>{expect(challengeCleared(119,120)).toBe(false);expect(challengeCleared(120,120)).toBe(true)})});
