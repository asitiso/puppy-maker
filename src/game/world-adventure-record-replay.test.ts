import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world adventure record replay',()=>{it('increments plays exactly once per scored accepted replay',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:500});const second=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:500});expect(second.adventureRecords.forest_path.plays-first.adventureRecords.forest_path.plays).toBe(1)})});
