import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure play count',()=>{it('counts scored attempts but not legacy unscored exploration',()=>{const legacy=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path'});expect(legacy.adventureRecords.forest_path.plays).toBe(0);const scored=worldReducer({...legacy,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:500});expect(scored.adventureRecords.forest_path.plays).toBe(1)})});
