import{describe,expect,it}from'vitest';import{initialWorldState,hydrateWorldState}from'./world-state';describe('world hydration fallback',()=>{it('returns a playable initial world state for corrupt json',()=>{const state=hydrateWorldState('{not-json');expect(state.screen).toBe(initialWorldState.screen);expect(state.adventureRecords).toEqual({});expect(state.adventureMasteries).toEqual([])})});
