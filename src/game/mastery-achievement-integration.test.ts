import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('mastery achievement integration',()=>{it('awards the five-master achievement once when the fifth mastery is earned',()=>{const prior=['moon_garden','village_market','old_shrine','starlight_hill'];const state={...initialWorldState,monthsCompleted:8,monthlyExplorations:0,adventureMasteries:prior,worldAchievements:[],adventureRecords:{}};const next=worldReducer(state,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(next.worldAchievements).toContain('adventure_apprentice');const gems=next.gems;const replay=worldReducer({...next,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(replay.worldAchievements.filter(id=>id==='adventure_apprentice')).toHaveLength(1);expect(replay.gems).toBe(gems)})});
