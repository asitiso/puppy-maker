import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world monthly goal isolation',()=>{it('clears claimed monthly goals on month transition',()=>{const state={...initialWorldState,screen:'result' as const,claimedMonthlyGoals:['wander','bond'],monthlyGoalCounters:{explore:2,bond:3}};const next=worldReducer(state,{type:'NEXT_MONTH'});expect(next.claimedMonthlyGoals).toEqual([]);expect(next.monthlyGoalCounters).toEqual({})})});
