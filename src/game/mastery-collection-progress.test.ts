import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('mastery collection progress',()=>{it('can accumulate mastery across monthly boundaries',()=>{let state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});state=worldReducer({...state,screen:'result'},{type:'NEXT_MONTH'});state=worldReducer({...state,monthsCompleted:2},{type:'EXPLORE',destinationId:'moon_garden',score:950,mastered:true});expect(state.adventureMasteries).toEqual(expect.arrayContaining(['forest_path','moon_garden']));expect(state.adventureMasteries).toHaveLength(2)})});
