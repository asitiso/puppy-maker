import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('mastery collection progress',()=>{it('can accumulate mastery across real unlock boundaries',()=>{let state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});state={...state,monthsCompleted:2,monthlyExplorations:0};state=worldReducer(state,{type:'EXPLORE',destinationId:'moon_garden',score:950,mastered:true});expect(state.adventureMasteries).toEqual(expect.arrayContaining(['forest_path','moon_garden']));expect(state.adventureMasteries).toHaveLength(2)})});
