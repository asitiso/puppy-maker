import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure collection preservation',()=>{it('does not remove an existing S id after a weaker replay',()=>{const state={...initialWorldState,adventureSGrades:['forest_path'],adventureBestScores:{forest_path:920},adventureRecords:{forest_path:{bestScore:920,bestGrade:'S' as const,firstCleared:true,mastered:false,plays:1}}};const next=worldReducer(state,{type:'EXPLORE',destinationId:'forest_path',score:100});expect(next.adventureSGrades).toEqual(['forest_path'])})});
