import{describe,expect,it}from'vitest';import{worldAchievements,newlyUnlockedAchievements}from'./world-achievements';import{initialWorldState}from'./world-state';describe('adventure mastery achievements',()=>{it('contains meaningful mastery milestones',()=>{expect(worldAchievements.some(a=>a.id==='adventure_apprentice')).toBe(true);expect(worldAchievements.some(a=>a.id==='adventure_master')).toBe(true)});it('unlocks mastery milestones from persistent adventure progress',()=>{const state={...initialWorldState,adventureMasteries:['a','b','c','d','e']};expect(newlyUnlockedAchievements(state).map(a=>a.id)).toContain('adventure_apprentice')})});
