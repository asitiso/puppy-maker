import{describe,expect,it}from'vitest';import{worldEndingBonuses}from'./world-narrative';describe('world ending legacy',()=>{it('turns mastery into bounded explorer and balanced legacy',()=>{const base={discoveredDestinations:[],huntChallengeClears:[],totalCooking:0,bondXp:0};expect(worldEndingBonuses({...base,adventureMasteries:['a','b','c']})).toMatchObject({explorer:24,balanced:9,guardian:6,sage:6,companion:3});expect(worldEndingBonuses({...base,adventureMasteries:Array(20).fill('x')}).explorer).toBe(80)})});
