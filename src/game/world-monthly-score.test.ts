import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world monthly score',()=>{it('adds score for a scored adventure',()=>{const next=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(next.monthlyWorldScore).toBeGreaterThan(0);expect(next.bestWorldScore).toBe(next.monthlyWorldScore)})});
