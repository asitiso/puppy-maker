import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world achievement refresh',()=>{it('can unlock discovery and mastery achievements in the same progression system',()=>{const state={...initialWorldState,monthsCompleted:8,discoveredDestinations:['moon_garden','village_market','old_shrine','starlight_hill'],adventureMasteries:['moon_garden','village_market','old_shrine','starlight_hill'],worldAchievements:[],monthlyExplorations:0};const next=worldReducer(state,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(next.worldAchievements).toEqual(expect.arrayContaining(['first_step','traveler','half_map','adventure_apprentice']))})});
