import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure meta progression isolation',()=>{it('keeps discovery run-scoped while keeping mastery meta-scoped',()=>{const mastered=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});const fresh=worldReducer(mastered,{type:'NEW_RUN'});expect(fresh.discoveredDestinations).not.toContain('forest_path');expect(fresh.adventureMasteries).toContain('forest_path');expect(fresh.adventureRecords.forest_path.firstCleared).toBe(true)})});
