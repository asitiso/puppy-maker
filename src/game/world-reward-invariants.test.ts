import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer,type WorldState}from'./world-state';describe('world reward nonnegative invariants',()=>{it('never makes gold negative through world message and month actions',()=>{let state:WorldState={...initialWorldState,gold:0,lastWorldMessage:'x',screen:'result'};state=worldReducer(state,{type:'CLEAR_WORLD_MESSAGE'});state=worldReducer(state,{type:'NEXT_MONTH'});expect(state.gold).toBeGreaterThanOrEqual(0)})});
