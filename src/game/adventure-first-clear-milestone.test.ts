import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure first clear milestone',()=>{it('makes first scored clear more rewarding than an equivalent replay',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:700});const replay=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:700});expect(first.gold-initialWorldState.gold).toBeGreaterThan(replay.gold-first.gold)})});
