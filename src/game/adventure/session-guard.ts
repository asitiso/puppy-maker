import type{AdventureSession}from'./runtime';export type SessionOutcome='playing'|'complete';export function canAcceptAdventureInput(session:AdventureSession){return !session.finished&&session.round<session.rounds}export function sessionOutcome(session:AdventureSession):SessionOutcome{return session.finished||session.round>=session.rounds?'complete':'playing'}export function safeProgress(session:AdventureSession){return Math.max(0,Math.min(1,session.round/Math.max(1,session.rounds)))}export function completionKey(destinationId:string,session:AdventureSession){return `${destinationId}:${session.rounds}:${session.score}:${session.mistakes}:${session.perfects}`}