import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'../world-state';describe('adventure replay contract',()=>{it('replay can improve score without duplicating mastery meta',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:700});const second=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:960,mastered:true});expect(second.adventureRecords.forest_path.bestScore).toBe(960);expect(second.adventureRecords.forest_path.plays).toBe(2);expect(second.adventureMasteries.filter(id=>id==='forest_path')).toHaveLength(1)})});