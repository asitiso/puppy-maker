import{describe,expect,it}from'vitest';import{destinations}from'../exploration';import{worldImpactFor}from'../world-narrative';describe('adventure world impact coverage',()=>{it('defines meaningful raising impact for every destination',()=>{for(const destination of destinations){const impact=worldImpactFor('explore',destination.id);expect(Boolean(impact.stats||impact.personality||impact.bondXp||impact.gold||impact.ingredients)).toBe(true)}})});
