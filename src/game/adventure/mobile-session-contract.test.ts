import{describe,expect,it}from'vitest';import{adventureStages}from'./catalog';describe('mobile adventure duration contract',()=>{it('keeps every normal adventure short enough for portrait mobile sessions',()=>{for(const stage of adventureStages){if(stage.destinationId==='guardian_sanctum')expect(stage.duration).toBeLessThanOrEqual(45);else expect(stage.duration).toBeLessThanOrEqual(30);expect(stage.duration).toBeGreaterThanOrEqual(10)}})});