import{describe,expect,it}from'vitest';import{createAdventureSession,choicePick,sessionRank}from'./runtime';describe('completion contract',()=>{it('clean execution can reach MASTER only after completion',()=>{let s=createAdventureSession('choice',3);s=choicePick(s,true);expect(s.finished).toBe(false);s=choicePick(s,true);s=choicePick(s,true);expect(s.finished).toBe(true);expect(sessionRank(s)).toBe('MASTER')});it('mistakes prevent accidental MASTER',()=>{let s=createAdventureSession('choice',2);s=choicePick(s,false);s=choicePick(s,true);expect(sessionRank(s)).not.toBe('MASTER')})});