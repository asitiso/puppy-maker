import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'../world-state';describe('monthly adventure attempts',()=>{it('restores attempts only after a real month transition',()=>{let s=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:800});s=worldReducer(s,{type:'EXPLORE',destinationId:'forest_path',score:800});expect(s.monthlyExplorations).toBe(2);const blocked=worldReducer(s,{type:'EXPLORE',destinationId:'forest_path',score:900});expect(blocked).toBe(s);const next=worldReducer({...s,screen:'result'},{type:'NEXT_MONTH'});expect(next.monthlyExplorations).toBe(0)})});