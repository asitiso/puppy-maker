import{describe,expect,it}from'vitest';import{createAdventureSession,timingTap}from'./runtime';import{canAcceptAdventureInput}from'./session-guard';describe('completed session input contract',()=>{it('exposes a hard stop signal after completion so UI cannot double-submit',()=>{let s=createAdventureSession('timing',1);s=timingTap(s,1);expect(s.finished).toBe(true);expect(canAcceptAdventureInput(s)).toBe(false)})});