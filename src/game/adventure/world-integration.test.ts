import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'../world-state';describe('adventure reward integration',()=>{it('does not consume an exploration until a completed minigame result is dispatched',()=>expect(initialWorldState.monthlyExplorations).toBe(0));it('scales rewards and records best scores from minigame grades',()=>{const c=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:300});const s=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:900});expect(s.gold).toBeGreaterThan(c.gold);expect(s.adventureBestScores.forest_path).toBe(900);expect(s.adventureSGrades).toContain('forest_path');expect(s.lastWorldMessage).toContain('S등급')});it('keeps only the best score across repeat visits',()=>{let s=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:800});s=worldReducer(s,{type:'EXPLORE',destinationId:'forest_path',score:400});expect(s.adventureBestScores.forest_path).toBe(800)})});
