import{describe,expect,it}from'vitest';import{choicePick,createAdventureSession,sessionRank}from'./runtime';describe('mastery rank boundary',()=>{it('distinguishes perfect mastery from merely successful play',()=>{let perfect=createAdventureSession('choice',4);for(let i=0;i<4;i++)perfect=choicePick(perfect,true);let mixed=createAdventureSession('choice',4);mixed=choicePick(mixed,true);mixed=choicePick(mixed,false);mixed=choicePick(mixed,true);mixed=choicePick(mixed,true);expect(sessionRank(perfect)).toBe('MASTER');expect(sessionRank(mixed)).not.toBe('MASTER')})});