import{describe,expect,it}from'vitest';import{availableAdventureStages}from'./catalog';describe('adventure unlock progression',()=>{it('never loses previously unlocked stages as months advance',()=>{let previous=0;for(let month=1;month<=12;month++){const count=availableAdventureStages(month).length;expect(count).toBeGreaterThanOrEqual(previous);previous=count}expect(previous).toBe(10)})});