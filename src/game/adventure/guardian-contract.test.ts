import{describe,expect,it}from'vitest';import{guardianPhase,guardianPhaseIndex,guardianTotalScore}from'./guardian';describe('guardian five-phase contract',()=>{it('moves through five ordered phases',()=>{expect([0,1,2,3,4].map(guardianPhase)).toHaveLength(5);expect(guardianPhaseIndex(0)).toBeGreaterThanOrEqual(0)});it('keeps total score monotonic',()=>{expect(guardianTotalScore([100,200,300])).toBeGreaterThanOrEqual(guardianTotalScore([100,200]))})});