import{describe,expect,it}from'vitest';import{hydrateWorldSave,serializeWorldState}from'../world-save';import{initialWorldState,worldReducer}from'../world-state';describe('adventure result persistence',()=>{it('round trips score, grade, plays and mastery',()=>{const played=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:955,mastered:true});const saved=hydrateWorldSave(serializeWorldState(played));expect(saved.adventureRecords.forest_path).toEqual(played.adventureRecords.forest_path);expect(saved.adventureMasteries).toContain('forest_path')})});