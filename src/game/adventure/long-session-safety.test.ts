import{describe,expect,it}from'vitest';import{choicePick,createAdventureSession}from'./runtime';describe('long session safety',()=>{it('handles repeated actions without NaN or infinite score',()=>{let s=createAdventureSession('choice',100);for(let i=0;i<100;i++)s=choicePick(s,i%3!==0);expect(Number.isFinite(s.score)).toBe(true);expect(s.finished).toBe(true);expect(s.round).toBe(100)})});