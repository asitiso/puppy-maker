import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';import{campaignReport}from'./campaign-report';describe('adventure progression campaign regression',()=>{it('keeps mastery as meta progression while monthly exploration resets',()=>{let state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:940,mastered:true});state=worldReducer(state,{type:'EXPLORE',destinationId:'moon_garden',score:910,mastered:true});expect(state.monthlyExplorations).toBe(2);const afterMonth=worldReducer({...state,screen:'result'},{type:'NEXT_MONTH'});expect(afterMonth.monthlyExplorations).toBe(0);expect(afterMonth.adventureMasteries).toEqual(expect.arrayContaining(['forest_path','moon_garden']));expect(afterMonth.adventureRecords.forest_path.bestScore).toBe(940)});it('raises campaign mastery completion without inflating world completion past 100',()=>{const state={...initialWorldState,adventureMasteries:['forest_path','moon_garden','village_market','old_shrine','starlight_hill'],adventureRecords:{}};const report=campaignReport(state);expect(report.adventureMastery).toBe(50);expect(report.worldCompletion).toBeLessThanOrEqual(100);expect(report.collectionCompletion).toBeLessThanOrEqual(100)});it('does not duplicate first-clear and master milestone rewards on repeat play',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:930,mastered:true});const replayBase={...first,monthlyExplorations:0};const replay=worldReducer(replayBase,{type:'EXPLORE',destinationId:'forest_path',score:930,mastered:true});expect(replay.adventureMasteries.filter(id=>id==='forest_path')).toHaveLength(1);expect(replay.adventureRecords.forest_path.plays).toBe(2);expect(replay.gems-first.gems).toBe(0)})});
