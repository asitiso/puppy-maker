import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('S grade milestone economy',()=>{it('pays S-grade gems once per destination',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:900});const replay=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:990});expect(first.adventureSGrades).toContain('forest_path');expect(replay.adventureSGrades.filter(id=>id==='forest_path')).toHaveLength(1);expect(replay.gems).toBe(first.gems)})});
