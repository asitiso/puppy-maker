import{describe,expect,it}from'vitest';import{canCook,consumeIngredients,recipes,unlockedRecipes}from'./cooking';describe('cooking',()=>{it('shows recipes progressively',()=>{expect(unlockedRecipes(1,[]).map(x=>x.id)).toEqual(['berry_milk']);expect(unlockedRecipes(10,['old_shrine'])).toHaveLength(5)});it('prevents ingredient underflow',()=>{const recipe=recipes[0];expect(canCook(recipe,{forest_berry:1,warm_milk:1})).toBe(true);expect(consumeIngredients(recipe,{forest_berry:0,warm_milk:1})).toBeNull()});it('consumes only required ingredients',()=>expect(consumeIngredients(recipes[0],{forest_berry:2,warm_milk:1,moon_herb:4})).toEqual({forest_berry:1,warm_milk:0,moon_herb:4}))});
