import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world action rejection safety',()=>{it('does not spend an exploration attempt on an unavailable destination',()=>{const next=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'not_real',score:999,mastered:true});expect(next).toBe(initialWorldState);expect(next.monthlyExplorations).toBe(0)})});
