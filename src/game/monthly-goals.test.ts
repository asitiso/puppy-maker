import{describe,expect,it}from'vitest';import{monthlyGoals,goalComplete,goalProgress}from'./monthly-goals';describe('monthly goals',()=>{it('tracks and caps goal progress',()=>{const goal=monthlyGoals[0];expect(goalProgress({explore:9},goal)).toBe(2);expect(goalComplete({explore:2},goal)).toBe(true)});it('requires actual target',()=>expect(goalComplete({cook:1},monthlyGoals[1])).toBe(false))});
