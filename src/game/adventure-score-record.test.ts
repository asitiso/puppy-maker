import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure score record',()=>{it('records zero as a scored attempt without treating it as missing input',()=>{const state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:0});expect(state.adventureRecords.forest_path.plays).toBe(1);expect(state.adventureRecords.forest_path.firstCleared).toBe(true);expect(state.adventureRecords.forest_path.bestScore).toBe(0)})});
