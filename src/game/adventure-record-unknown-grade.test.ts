import{describe,expect,it}from'vitest';import{initialWorldState,hydrateWorldState}from'./world-state';describe('adventure record unknown grade migration',()=>{it('derives grade from score when persisted grade is unknown',()=>{const state=hydrateWorldState(JSON.stringify({...initialWorldState,adventureRecords:{forest_path:{bestScore:950,bestGrade:'legendary',firstCleared:true,mastered:false,plays:1}}}));expect(state.adventureRecords.forest_path.bestGrade).toBe('S')})});
