import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world adventure first discovery',()=>{it('does not duplicate discovery ids on replay',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:500});const second=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:600});expect(second.discoveredDestinations.filter(id=>id==='forest_path')).toHaveLength(1)})});
