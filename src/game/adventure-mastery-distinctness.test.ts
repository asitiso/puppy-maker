import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure mastery distinctness',()=>{it('counts distinct mastery destinations only once through normal reducer flow',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});const second=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:980,mastered:true});expect(second.adventureMasteries).toEqual(['forest_path'])})});
