export type GoalKind='explore'|'cook'|'train'|'bond';export interface MonthlyGoal{id:string;title:string;kind:GoalKind;target:number;rewardGold:number}
export const monthlyGoals:MonthlyGoal[]=[{id:'wander',title:'루나와 두 번 탐험하기',kind:'explore',target:2,rewardGold:180},{id:'chef',title:'따뜻한 요리 만들기',kind:'cook',target:2,rewardGold:220},{id:'practice',title:'훈련 세 번 마치기',kind:'train',target:3,rewardGold:250},{id:'together',title:'루나와 교감하기',kind:'bond',target:2,rewardGold:160}];
export const goalById=(id:string)=>monthlyGoals.find(g=>g.id===id);export const goalProgress=(counters:Partial<Record<GoalKind,number>>,goal:MonthlyGoal)=>Math.min(goal.target,counters[goal.kind]??0);export const goalComplete=(counters:Partial<Record<GoalKind,number>>,goal:MonthlyGoal)=>goalProgress(counters,goal)>=goal.target;
