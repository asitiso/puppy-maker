import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure discovery compatibility',()=>{it('keeps first discovery semantics while using scored mini-games',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:700});expect(first.discoveredDestinations).toEqual(['forest_path']);const repeat=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:800});expect(repeat.discoveredDestinations).toEqual(['forest_path'])})});
