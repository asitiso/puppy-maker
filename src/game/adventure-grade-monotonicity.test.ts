import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure grade monotonicity',()=>{it('upgrades best grade when later play improves',()=>{const c=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:100});const s=worldReducer({...c,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:950});expect(c.adventureRecords.forest_path.bestGrade).toBe('C');expect(s.adventureRecords.forest_path.bestGrade).toBe('S')})});
