import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure reward tiers',()=>{it('rewards stronger grades more than weak grades for the same destination',()=>{const c=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:100});const s=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950});expect(s.gold).toBeGreaterThan(c.gold);expect(s.adventureRecords.forest_path.bestGrade).toBe('S');expect(c.adventureRecords.forest_path.bestGrade).toBe('C')})});
