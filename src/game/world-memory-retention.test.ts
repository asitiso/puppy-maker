import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world moment retention',()=>{it('keeps only the latest 24 world memories during a long campaign',()=>{let state=initialWorldState;for(let i=0;i<30;i++){state=worldReducer({...state,monthlyBondActions:0},{type:'BOND'})}expect(state.worldMoments).toHaveLength(24);expect(state.bestWorldScore).toBeGreaterThanOrEqual(state.monthlyWorldScore)})});
