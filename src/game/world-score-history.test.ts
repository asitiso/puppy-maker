import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world score history',()=>{it('updates best world score when a new action pushes the monthly score higher',()=>{const state={...initialWorldState,monthlyWorldScore:90,bestWorldScore:95,monthlyBondActions:0};const next=worldReducer(state,{type:'BOND'});expect(next.bestWorldScore).toBeGreaterThanOrEqual(100)})});
