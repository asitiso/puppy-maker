import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world cumulative counters',()=>{it('keeps cumulative exploration count across month reset',()=>{const state={...initialWorldState,screen:'result' as const,totalExplorations:11,monthlyExplorations:2};const next=worldReducer(state,{type:'NEXT_MONTH'});expect(next.totalExplorations).toBe(11);expect(next.monthlyExplorations).toBe(0)})});
