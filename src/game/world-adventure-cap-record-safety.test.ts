import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world adventure cap record safety',()=>{it('does not increment play count after monthly cap',()=>{const base={...initialWorldState,monthlyExplorations:2,adventureRecords:{forest_path:{bestScore:500,bestGrade:'B' as const,firstCleared:true,mastered:false,plays:2}}};const next=worldReducer(base,{type:'EXPLORE',destinationId:'forest_path',score:900});expect(next.adventureRecords.forest_path.plays).toBe(2)})});
