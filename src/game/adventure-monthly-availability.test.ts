import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure monthly availability',()=>{it('restores adventure attempts after a valid month transition',()=>{const used={...initialWorldState,screen:'result' as const,monthlyExplorations:2};const next=worldReducer(used,{type:'NEXT_MONTH'});expect(next.monthlyExplorations).toBe(0)})});
