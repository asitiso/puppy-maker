import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure first clear flag',()=>{it('keeps first-cleared true after all subsequent scored replays',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:1});const later=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:0});expect(later.adventureRecords.forest_path.firstCleared).toBe(true)})});
