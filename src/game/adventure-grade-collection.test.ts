import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure grade collection',()=>{it('adds an S id only when S is reached',()=>{const a=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:700});expect(a.adventureSGrades).not.toContain('forest_path');const s=worldReducer({...a,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:950});expect(s.adventureSGrades).toContain('forest_path')})});
