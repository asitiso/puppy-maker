import{describe,expect,it}from'vitest';import{initialWorldState,hydrateWorldState}from'./world-state';describe('wardrobe migration default',()=>{it('keeps the classic Runa wardrobe available when old saves lack wardrobe data',()=>{const state=hydrateWorldState(JSON.stringify({...initialWorldState,wardrobeUnlocks:undefined}));expect(state.wardrobeUnlocks).toContain('runa_classic')})});
