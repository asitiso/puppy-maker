import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world feedback progression',()=>{it('writes a memory for each accepted exploration',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:700});const second=worldReducer({...first,monthsCompleted:2},{type:'EXPLORE',destinationId:'moon_garden',score:800});expect(second.worldMoments.filter(m=>m.kind==='explore')).toHaveLength(2)})});
