import{describe,expect,it}from'vitest';import{initialWorldState,hydrateWorldState}from'./world-state';describe('duplicate meta sanitation',()=>{it('deduplicates legacy S grades and mastery ids',()=>{const state=hydrateWorldState(JSON.stringify({...initialWorldState,adventureSGrades:['forest_path','forest_path'],adventureMasteries:['forest_path','forest_path']}));expect(state.adventureSGrades).toEqual(['forest_path']);expect(state.adventureMasteries).toEqual(['forest_path'])})});
