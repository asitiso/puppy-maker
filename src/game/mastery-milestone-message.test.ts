import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('mastery milestone feedback',()=>{it('persists MASTER in the adventure memory while first-discovery achievement owns the immediate toast',()=>{const state=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(state.lastWorldMessage).toContain('첫 발자국');expect(state.worldMoments.at(-1)?.summary).toContain('MASTER');expect(state.adventureMasteries).toContain('forest_path')})});
