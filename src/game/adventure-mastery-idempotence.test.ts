import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure mastery idempotence',()=>{it('does not duplicate mastery when a mastered stage is replayed with a lower result',()=>{const mastered=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});const replay=worldReducer({...mastered,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:400,mastered:false});expect(replay.adventureMasteries).toEqual(['forest_path']);expect(replay.adventureRecords.forest_path.mastered).toBe(true)})});
