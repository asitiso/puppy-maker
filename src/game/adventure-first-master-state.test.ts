import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure first master state',()=>{it('can upgrade an already-cleared stage to MASTER later',()=>{const clear=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:700,mastered:false});const master=worldReducer({...clear,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:930,mastered:true});expect(master.adventureRecords.forest_path).toMatchObject({firstCleared:true,mastered:true,plays:2});expect(master.adventureMasteries).toContain('forest_path')})});
