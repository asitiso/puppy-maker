import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world adventure monthly score reset',()=>{it('resets monthly score after result month while retaining best score',()=>{const explored=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:800});const best=explored.bestWorldScore;const next=worldReducer({...explored,screen:'result'},{type:'NEXT_MONTH'});expect(next.monthlyWorldScore).toBe(0);expect(next.bestWorldScore).toBe(best)})});
