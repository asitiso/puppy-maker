import{describe,expect,it}from'vitest';import{initialWorldState,hydrateWorldState}from'./world-state';describe('legacy score migration',()=>{it('derives a non-master record from legacy best score and S list',()=>{const state=hydrateWorldState(JSON.stringify({...initialWorldState,adventureBestScores:{forest_path:920},adventureSGrades:['forest_path']}));expect(state.adventureRecords.forest_path).toEqual({bestScore:920,bestGrade:'S',firstCleared:true,mastered:false,plays:1})})});
