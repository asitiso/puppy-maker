import{describe,expect,it}from'vitest';import{hydrateWorldSave,serializeWorldState,WORLD_SAVE_VERSION}from'./world-save';import{initialWorldState}from'./world-state';describe('legacy world save matrix',()=>{for(const version of[1,2,3,4,5,6])it(`hydrates v${version} core state into v${WORLD_SAVE_VERSION}`,()=>{const raw=JSON.stringify({version,state:{...initialWorldState,gold:4321,monthsCompleted:4,discoveredDestinations:['forest_path'],adventureBestScores:{forest_path:880},adventureSGrades:version>=6?['forest_path']:[]}});const hydrated=hydrateWorldSave(raw);expect(hydrated.gold).toBe(4321);expect(hydrated.monthsCompleted).toBe(4);expect(hydrated.discoveredDestinations).toContain('forest_path');expect(hydrated.adventureRecords.forest_path.bestScore).toBe(880)});it('serializes current version after migration',()=>{const migrated=hydrateWorldSave(JSON.stringify({version:4,state:{...initialWorldState,gold:999}}));expect(JSON.parse(serializeWorldState(migrated)).version).toBe(WORLD_SAVE_VERSION)})});
