import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('mastery reward economy',()=>{it('pays mastery milestone only on the first master clear',()=>{const first=worldReducer(initialWorldState,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});const firstGain=first.gold-initialWorldState.gold;const repeat=worldReducer({...first,monthlyExplorations:0},{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});const repeatGain=repeat.gold-first.gold;expect(firstGain).toBeGreaterThan(repeatGain)})});
