import{describe,expect,it}from'vitest';import{initialWorldState,hydrateWorldState}from'./world-state';describe('world defaults migration',()=>{it('adds all current world progression containers to a core-only legacy state',()=>{const state=hydrateWorldState(JSON.stringify({...initialWorldState,adventureBestScores:undefined,adventureSGrades:undefined}));expect(state).toMatchObject({adventureBestScores:{},adventureSGrades:[],adventureRecords:{},adventureMasteries:[],worldMoments:[],monthlyWorldScore:0})})});
