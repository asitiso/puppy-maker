import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('adventure mastery achievement economy',()=>{it('adds the mastery achievement reward on the fifth distinct master',()=>{const prior=['moon_garden','village_market','old_shrine','starlight_hill'];const base={...initialWorldState,monthsCompleted:8,adventureMasteries:prior,worldAchievements:['first_step','traveler'],discoveredDestinations:prior,monthlyExplorations:0};const next=worldReducer(base,{type:'EXPLORE',destinationId:'forest_path',score:950,mastered:true});expect(next.worldAchievements).toContain('adventure_apprentice');expect(next.gems).toBeGreaterThan(base.gems)})});
