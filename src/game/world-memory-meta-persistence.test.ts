import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world memory meta persistence',()=>{it('preserves recent memories across a new run',()=>{const before={...initialWorldState,worldMoments:[{kind:'explore' as const,sourceId:'forest_path',month:2,title:'숲길',summary:'기억'}]};const after=worldReducer(before,{type:'NEW_RUN'});expect(after.worldMoments).toEqual(before.worldMoments)})});
