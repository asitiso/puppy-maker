import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('world exploration counter',()=>{it('increments cumulative exploration for scored and compatible legacy exploration',()=>{const ready={...initialWorldState,monthsCompleted:2};const one=worldReducer(ready,{type:'EXPLORE',destinationId:'forest_path'});const two=worldReducer(one,{type:'EXPLORE',destinationId:'moon_garden',score:700});expect(two.totalExplorations).toBe(2)})});
