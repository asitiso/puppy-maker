import{describe,expect,it}from'vitest';import{initialWorldState,worldReducer}from'./world-state';describe('monthly world boundaries',()=>{it('resets monthly counters but not cumulative counters on month advance',()=>{const before={...initialWorldState,screen:'result' as const,monthlyExplorations:2,monthlyBondActions:3,monthlyGoalCounters:{explore:2,bond:3},claimedMonthlyGoals:['wander'],monthlyWorldScore:99,totalExplorations:7,totalCooking:4,bondXp:22};const after=worldReducer(before,{type:'NEXT_MONTH'});expect(after).toMatchObject({monthlyExplorations:0,monthlyBondActions:0,monthlyGoalCounters:{},claimedMonthlyGoals:[],monthlyWorldScore:0,totalExplorations:7,totalCooking:4,bondXp:22})})});
